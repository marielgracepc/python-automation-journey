
---

# **Day 3 – If Statements**

```markdown
# Day 3 – If Statements

## Objective
Learn conditional statements in Python.

## What I Learned
- `if`, `elif`, `else` syntax
- Comparison operators (`==`, `!=`, `<`, `>`)
- Indentation is required in Python

## Practice Example
```python
subject = "ECO Release Notify:001"
if subject.startswith("ECO"):
    print("Matched")
else:
    print("Not Matched")
